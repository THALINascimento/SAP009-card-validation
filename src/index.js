import validator from './validator.js';

console.log(validator);

Document.queryselector('numero do cartoa').oninput = () =>{
    document.queryselector('numero do cartao').innerText = document.querySelector('numero do cartoa').value;
}