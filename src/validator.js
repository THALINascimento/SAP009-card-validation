export function isValid(validar) { //objeto
  const card = document.getElementById("numero").value;
  const invert = card.split("").reverse();
  console.log(invert); //numeros digitados invertidos tem outro jeito?; criar var arr () = invert const cardArray = []; //como colocar no arr para manipular posições ?
  //const newNumber = parseInt(?[i]) + parseInt(?[1]);
  
  let soma = 0;
  const multiplicar = invert[i] *2;
  const total = soma + multiplicar;
  
  for (let i = 0; i < invert.length; i= i ++) {//let digito = parseInt(validar[i]);
  if (i % 2 === 0) {//((i + 1) % 2 !== 0); {simplificar se posicao par multiplicar se posicao impar some (i !== 0 && i % 2 === 0)
  multiplicar;

  if (multiplicar > 9) {
  soma = (soma + (multiplicar - 9 ));
  } 
     
  // if { 
  // soma = (soma + multiplicar),
  //}
  //if {
  //soma = soma + invert[i];
  //}
  if (total % 10 === 0) {
  return true; // return soma % 10 === 0;
  } else {
  return false;
}
}
}
export function maskify(validar) {
 // maskify: function(){}
  const maskify =[];
  for (let i = 0; i < invert.length; i=i ++) {
        if (i < numeroCartao.length; - 4) { //simplificar se posicao par multiplicar se posicao impar some (i !== 0 && i % 2 === 0)
          maskify.push("#")
      invert[i] = invert[i] *= 2;
  
    } if { 
      //invert[i] > 9 
      //invert[i] = invert[i] - 9;
      //maskify.push(validar[i]);
      
  return maskify.join("");
  return total % 10 === 0
    
}
}
}
}


 //export default validator;